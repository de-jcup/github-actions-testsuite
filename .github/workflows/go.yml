# SPDX-License-Identifier: MIT
name: github-actions go test 1

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v2-beta
      with:
        go-version: '1.14.1'
    